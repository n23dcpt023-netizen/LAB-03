<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            text-decoration: none;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #ff7f32, #008cff);
        }

        .login-container {
            background-color: #fff;
            width: 400px;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0px 20px 40px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .login-container h2 {
            font-size: 26px;
            margin-bottom: 30px;
            font-weight: 600;
        }

        .input-group {
            margin-bottom: 25px;
            position: relative;
            text-align: left;
        }

        .input-group label {
            display: block;
            font-size: 14px;
            color: #777;
            margin-bottom: 6px;
        }

        .input-group input {
            width: 100%;
            padding: 12px 40px;
            border: none;
            border-bottom: 1px solid #ccc;
            font-size: 14px;
            outline: none;
        }

        .input-group i {
            position: absolute;
            top: 36px;
            left: 10px;
            font-size: 16px;
            color: #666;
        }

        .forgot-link {
            font-size: 12px;
            position: absolute;
            right: 0;
            bottom: -18px;
            text-decoration: none;
            color: #888;
        }

        .login-btn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(120deg, #4facfe, #8f44fd);
            color: white;
            border: none;
            border-radius: 25px;
            font-size: 16px;
            font-weight: bold;
            margin: 30px 0;
            cursor: pointer;
            transition: 0.3s;
        }

        .login-btn:hover {
            opacity: 0.9;
        }

        .social-text {
            font-size: 14px;
            color: #555;
            margin-bottom: 18px;
            text-decoration: none;
        }

        .social-icons {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
    margin: 10px 0;
}

.social-icons i {
    font-size: 18px;
    color: white;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
}

.social-icons .fa-facebook-f {
    background-color: #1877F2;
}

.social-icons .fa-twitter {
    background-color: #1DA1F2;
}

.social-icons .fa-google {
    background-color: #DB4437;
}

        .signup-section {
            margin-top: 30px;
        }

        .signup-section p {
            font-size: 14px;
            color: #555;
        }

        .signup-section a {
            font-weight: bold;
            color: #444;
           
        }
    </style>
</head>
<body>

    <div class="login-container">
        <h2>Login</h2>

        <div class="input-group">
            <label>Username</label>
            <i class="fa-regular fa-user"></i>
            <input type="text" id="username" placeholder="Type your username">
        </div>

        <div class="input-group">
            <label>Password</label>
            <i class="fa-solid fa-lock"></i>
            <input type="password" id="password" placeholder="Type your password">
            <a href="#" class="forgot-link">Forgot password?</a>
        </div>

        <button class="login-btn" onclick="handleLogin()">LOGIN</button>

        <div class="social-text">Or Sign Up Using</div>
        <div class="social-icons">
            <a href="https://www.facebook.com/"><i class="fa-brands fa-facebook-f"></i></a>
            <a href="https://www.twitter.com"><i class="fa-brands fa-twitter"></i></a>
            <a href="https://www.google.com"><i class="fa-brands fa-google"></i></a>
        </div>

        <div class="signup-section">
            <p>Or Sign Up Using</p>
            <a href="#">SIGN UP</a>
        </div>
    </div>

    <script>
        function handleLogin() {
            const username = document.getElementById("username").value.trim();
            const password = document.getElementById("password").value.trim();

            if (!username && !password) {
                alert("Please enter your Username va Password!");
            } else if (!username) {
                alert("Please enter your Username!");
            } else if (!password) {
                alert("Please enter your Password!");
            } else {
                alert("Login success (gia lap)!");
            }
        }
    </script>
</body>
</html>
